// Dog interface representing a dog object
export interface Dog {
    id: string;
    img: string;
    name: string;
    age: number;
    zip_code: string;
    breed: string;
}
  
// Interface for the match result
export interface Match {
    match: string;
}